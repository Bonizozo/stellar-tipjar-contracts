{
  "name": "@tipjar/contract-client",
  "version": "0.1.0",
  "description": "Frontend contract client for Stellar TipJar with generated TypeScript bindings.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "generate-bindings": "bash ../../scripts/generate-bindings.sh",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^12.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
